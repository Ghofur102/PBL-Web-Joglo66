<IfModule mod_rewrite.c>
    RewriteEngine On

    # Pastikan tidak membaca ulang folder public jika sudah di dalam
    RewriteCond %{REQUEST_URI} !^/public/

    # Alihkan semua request ke folder public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
